// Each test must use the entirety of this module to avoid unused warnings,
// which is unreasonable. So we ignore these kind of warnings.
#![allow(dead_code)]

pub(crate) type Id = u32;

pub mod user;
